{
  "name": "foundry-raffle-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:deployments": "node scripts/generate-deployments.js",
    "dev": "npm run sync:deployments && vite",
    "build": "npm run sync:deployments && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ethers": "^6.10.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
